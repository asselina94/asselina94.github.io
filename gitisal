#include <stdio.h>
int my_isalnum(char a){
    if ((int)a >= (int)'A' && (int)a <= (int)'Z') {
        return 1;
    }
    if((int)a >= (int)'a' && (int)a <= (int)'z'){
        return 1;
    }
    if( a >= 48 &&  a <= 57) {
        return 1;
       }
    else{
        return 0;
    }
}
