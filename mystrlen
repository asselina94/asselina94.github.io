#include <stdio.h>
int my_strlen(char* a) {
    int c=0;
    while(*a!='\0'){
        c++;
        *a++;
    }

return (c);
  
}

//swap
#include <stdio.h>
void my_swap(int* a, int* b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}
